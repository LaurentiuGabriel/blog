---
layout: layouts/base.njk
---
<article class="prose">
  {% if title and showTitle != false %}
    <h1 class="page-title">{{ title }}</h1>
    {% if tagline %}<p class="page-tagline">{{ tagline }}</p>{% endif %}
  {% endif %}
  {{ content | safe }}
</article>
